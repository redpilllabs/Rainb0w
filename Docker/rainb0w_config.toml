# Rainb0w config file
[DOMAINS]
# This domain will be used for the decoy website.
MAIN_DOMAIN = ""
# Used for TCP/UDP connections such as Hysteria that are not compatible with CDNs.
DIRECT_CONN_DOMAIN = ""
# Used for protocols that are CDN compatible (Websocket, HTTPUpgrade, gRPC)
CDN_COMPAT_DOMAIN = ""


[CLOUDFLARE]
# This key will help Caddy's Cloudflare DNS plugin to verify your domain using DNS-01 challenges and obtain TLS certs
CLOUDFLARE_API_KEY = ""
